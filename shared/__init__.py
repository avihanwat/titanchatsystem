"""
Shared modules — organized into sub-packages:

  shared.db            — Cassandra database client
  shared.cache         — Redis: connection registry, chat tracker, agent router, rate limiter
  shared.auth          — JWT authentication and authorization
  shared.observability — Logging, telemetry (OpenTelemetry), trace context
"""
